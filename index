<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Members Lounge | Complete Event Suite</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --deep-charcoal: #1E1E1E;
      --warm-gold: #C6A43F;
      --soft-gold: rgba(198,164,63,0.15);
      --cream: #FAF7F2;
      --pure-white: #FFFFFF;
      --light-gray: #F0F0F0;
      --muted-text: #6B6B6B;
      --shadow-sm: 0 4px 12px rgba(0,0,0,0.05);
      --shadow-md: 0 8px 24px rgba(0,0,0,0.1);
      --shadow-lg: 0 16px 32px rgba(0,0,0,0.12);
      --radius-md: 12px;
      --radius-lg: 20px;
      --transition: all 0.2s ease;
    }
    html, body { height: 100%; overflow: hidden; }
    body {
      display: flex;
      flex-direction: column;
      height: 100vh;
      font-family: 'Inter', sans-serif;
      background-color: var(--cream);
      color: var(--deep-charcoal);
      line-height: 1.5;
    }
    h1, h2, h3, h4 { font-family: 'Playfair Display', serif; font-weight: 700; }
    .gold { color: var(--warm-gold); }
    header {
      flex-shrink: 0;
      background: var(--pure-white);
      padding: 20px;
      text-align: center;
      border-bottom: 1px solid rgba(0,0,0,0.05);
      box-shadow: var(--shadow-sm);
    }
    .brandline { display: flex; align-items: center; justify-content: center; gap: 20px; flex-wrap: wrap; }
    .logoWrap { display: flex; align-items: center; justify-content: center; padding: 10px 20px; background: var(--pure-white); border-radius: 60px; box-shadow: var(--shadow-sm); }
    .logoImg { max-height: 60px; width: auto; display: block; }
    .logoFallback { display: inline-flex; align-items: center; gap: 12px; font-weight: 700; letter-spacing: 0.1em; font-size: 14px; color: var(--deep-charcoal); }
    h1 { margin: 10px 0 5px; font-size: 2rem; font-weight: 900; }
    .sub { color: var(--muted-text); font-size: 0.95rem; }
    .tabs-wrap {
      flex-shrink: 0;
      background: rgba(255,255,255,0.95);
      border-bottom: 1px solid rgba(0,0,0,0.05);
      padding: 12px 0;
    }
    .tabs {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      justify-content: center;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }
    .tab {
      cursor: pointer;
      user-select: none;
      padding: 8px 20px;
      border-radius: 40px;
      font-size: 0.9rem;
      font-weight: 600;
      background: transparent;
      color: var(--muted-text);
      border: 1px solid transparent;
      transition: var(--transition);
      white-space: nowrap;
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .tab:hover { color: var(--warm-gold); background: var(--soft-gold); border-color: var(--soft-gold); }
    .tab.active { background: var(--warm-gold); color: white; border-color: var(--warm-gold); box-shadow: var(--shadow-sm); }
    .tab.nextTab { background: var(--soft-gold); color: var(--warm-gold); border-color: var(--warm-gold); font-weight: 700; }
    .page { display: none; }
    .page.active { display: block; }
    main {
      flex: 1 1 auto;
      overflow-y: auto;
      padding: 20px;
      max-width: 1400px;
      width: 100%;
      margin: 0 auto;
    }
    .card {
      background: var(--pure-white);
      border: none;
      border-radius: var(--radius-lg);
      padding: 30px;
      box-shadow: var(--shadow-md);
      transition: var(--transition);
      margin-bottom: 20px;
    }
    .card h2 { font-size: 1.5rem; margin-bottom: 15px; display: flex; align-items: center; gap: 10px; }
    .hint { color: var(--muted-text); font-size: 0.9rem; margin-bottom: 20px; font-style: italic; }
    .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    @media (max-width: 920px) { .grid { grid-template-columns: 1fr; } }
    label {
      display: block;
      font-size: 0.85rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      color: var(--muted-text);
      margin-bottom: 6px;
    }
    input, select, textarea {
      width: 100%;
      padding: 12px 16px;
      border-radius: var(--radius-md);
      border: 1px solid #E5E5E5;
      background: var(--pure-white);
      font-size: 1rem;
      font-family: 'Inter', sans-serif;
      transition: var(--transition);
      margin-bottom: 20px;
    }
    input:focus, select:focus, textarea:focus {
      outline: none;
      border-color: var(--warm-gold);
      box-shadow: 0 0 0 3px var(--soft-gold);
    }
    .row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    .actions {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      margin-top: 25px;
    }
    .btn {
      cursor: pointer;
      border: none;
      background: var(--pure-white);
      color: var(--deep-charcoal);
      padding: 10px 22px;
      border-radius: 40px;
      font-size: 0.9rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
      border: 1px solid #E5E5E5;
      min-width: 120px;
    }
    .btn:hover { background: var(--soft-gold); border-color: var(--warm-gold); transform: translateY(-2px); }
    .btn.primary { background: var(--warm-gold); color: white; border-color: var(--warm-gold); }
    .btn.primary:hover { background: #b18f35; }
    .btn.ghost { background: transparent; border: 1px solid var(--warm-gold); color: var(--warm-gold); }
    .btn.small { padding: 6px 12px; font-size: 0.8rem; min-width: auto; }
    .table {
      width: 100%;
      border-collapse: collapse;
      font-size: 1rem;
      margin: 20px 0;
    }
    .table td { padding: 12px 0; border-bottom: 1px solid #F0F0F0; }
    .table td:last-child { text-align: right; font-weight: 700; color: var(--warm-gold); }
    .total {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 18px 20px;
      background: var(--soft-gold);
      border-radius: var(--radius-md);
      font-size: 1.2rem;
      font-weight: 700;
      margin-top: 20px;
    }
    .total div:last-child { color: var(--warm-gold); font-size: 1.4rem; }
    .kpi {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
      gap: 20px;
      margin-top: 25px;
    }
    .kpi .box {
      background: var(--pure-white);
      border: 1px solid #F0F0F0;
      border-radius: var(--radius-md);
      padding: 15px;
      text-align: center;
      box-shadow: var(--shadow-sm);
    }
    .kpi .box .label { font-size: 0.75rem; text-transform: uppercase; color: var(--muted-text); margin-bottom: 6px; }
    .kpi .box .value { font-size: 1.4rem; font-weight: 700; color: var(--deep-charcoal); }
    .course { margin-top: 30px; padding-top: 20px; border-top: 2px dashed #F0F0F0; }
    .course h3 { font-size: 1.3rem; margin-bottom: 15px; display: flex; align-items: center; gap: 10px; }
    .optionsGrid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 15px;
      margin: 15px 0;
    }
    .opt {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 14px;
      background: var(--pure-white);
      border: 1px solid #F0F0F0;
      border-radius: var(--radius-md);
      transition: var(--transition);
      cursor: pointer;
    }
    .opt input[type="checkbox"] { width: 18px; height: 18px; margin-top: 2px; accent-color: var(--warm-gold); flex-shrink: 0; }
    .optNum {
      flex-shrink: 0;
      width: 26px;
      height: 26px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: var(--soft-gold);
      border-radius: 50%;
      font-weight: 700;
      font-size: 0.85rem;
      color: var(--warm-gold);
    }
    .optLabel { flex: 1; font-weight: 600; font-size: 0.95rem; line-height: 1.4; }
    .hotTag {
      display: inline-block;
      margin-left: 8px;
      padding: 2px 8px;
      background: rgba(198,164,63,0.15);
      border-radius: 30px;
      font-size: 0.65rem;
      font-weight: 700;
      text-transform: uppercase;
      color: var(--warm-gold);
    }
    .count { font-size: 0.9rem; color: var(--muted-text); margin: 10px 0 5px; }
    .warnBox {
      margin-top: 10px;
      padding: 10px 12px;
      border-radius: var(--radius-md);
      background: #fff3e0;
      border-left: 4px solid #f5a623;
      color: #8a6d2b;
      display: none;
      font-weight: 600;
      font-size: 0.9rem;
    }
    .statusRow { display: flex; gap: 12px; flex-wrap: wrap; margin: 20px 0; }
    .badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 14px;
      border-radius: 40px;
      font-size: 0.85rem;
      font-weight: 600;
      background: var(--soft-gold);
      color: var(--warm-gold);
    }
    .badge.ok { background: #e3f5e9; color: #1e7b4c; }
    .badge.warn { background: #fff3e0; color: #b45b0a; }
    .pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 14px;
      border-radius: 40px;
      background: var(--soft-gold);
      color: var(--deep-charcoal);
      font-size: 0.85rem;
      margin: 0 6px 6px 0;
      font-weight: 500;
    }
    .summary-section { margin: 20px 0; }
    .summary-section h3 { font-size: 1.2rem; margin: 20px 0 10px; color: var(--warm-gold); border-bottom: 1px dashed #E5E5E5; padding-bottom: 5px; }
    .summary-line { display: flex; justify-content: space-between; font-size: 0.95rem; padding: 8px 0; gap: 20px; }
    .summary-line.total { font-weight: 700; border-top: 2px solid var(--warm-gold); margin-top: 20px; padding-top: 20px; font-size: 1.2rem; }
    .summary-items { list-style: none; padding: 0; margin: 10px 0; }
    .summary-items li { padding: 4px 0; color: var(--muted-text); }
    #chips { display: flex; flex-wrap: wrap; margin: 15px 0; }
    footer {
      flex-shrink: 0;
      text-align: center;
      padding: 15px 20px;
      color: var(--muted-text);
      font-size: 0.85rem;
      border-top: 1px solid rgba(0,0,0,0.05);
      background: var(--pure-white);
    }
    .table-builder-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 16px;
      margin-top: 20px;
    }
    .table-card {
      background: var(--pure-white);
      border: 1px solid #E5E5E5;
      border-radius: var(--radius-md);
      padding: 12px;
    }
    .table-title { font-weight: 700; margin-bottom: 10px; color: var(--deep-charcoal); font-size: 0.9rem; }
    .table-controls { display: flex; gap: 8px; }
    .table-controls select { width: 50%; padding: 6px; margin-bottom: 0; font-size: 0.8rem; }
    .dietary-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin: 8px 0;
    }
    .dietary-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      min-width: 70px;
      background: var(--soft-gold);
      padding: 4px 6px;
      border-radius: var(--radius-md);
    }
    .dietary-item label { font-size: 0.65rem; text-transform: none; font-weight: 600; margin-bottom: 2px; color: var(--deep-charcoal); }
    .dietary-item input { width: 55px; padding: 4px; text-align: center; margin-bottom: 0; font-size: 0.75rem; }
    .run-sheet-table { width: 100%; border-collapse: collapse; }
    .run-sheet-table th, .run-sheet-table td { border: 1px solid #E5E5E5; padding: 8px; text-align: left; vertical-align: middle; }
    .run-sheet-table select, .run-sheet-table input { margin-bottom: 0; padding: 6px; width: auto; min-width: 80px; }
    .guest-list-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin: 8px 0;
    }
    .guest-name-input {
      width: calc(33% - 6px);
      padding: 4px;
      font-size: 0.7rem;
      margin-bottom: 0;
    }
    @media (max-width: 768px) {
      h1 { font-size: 1.6rem; }
      .tabs { overflow-x: auto; justify-content: flex-start; }
      .row { grid-template-columns: 1fr; }
      .card { padding: 20px; }
      .btn { min-width: 100%; }
      .guest-name-input { width: calc(50% - 6px); }
    }
    @media print { .tabs-wrap, .actions, .btn, footer { display: none !important; } body { background: white; overflow: visible; } main { overflow: visible; } }
  </style>
</head>

<body>
  <header>
    <div class="brandline">
      <div class="logoWrap">
        <img class="logoImg" id="brandLogo" src="members_lounge_logo.png" alt="Brand Logo"
             onerror="this.style.display='none'; document.getElementById('logoFallback').style.display='inline-flex';">
        <span class="logoFallback" id="logoFallback" style="display:none;">
          <i class="fas fa-crown" style="color: var(--warm-gold);"></i>
          <span>THE MEMBERS LOUNGE</span>
        </span>
      </div>
    </div>
    <h1><span class="gold" id="brandTitle">Event Signature Selection</span></h1>
    <div class="sub" id="brandSubtitle">Reggio Calabria Club, Parkville</div>
  </header>

  <div class="tabs-wrap">
    <div class="tabs" role="tablist">
      <div class="tab active" data-page="quote"><i class="fas fa-file-invoice"></i> Quote</div>
      <div class="tab" data-page="couple"><i class="fas fa-heart"></i> Couple</div>
      <div class="tab" data-page="food"><i class="fas fa-utensils"></i> Food</div>
      <div class="tab" data-page="drinks"><i class="fas fa-wine-glass-alt"></i> Drinks</div>
      <div class="tab" data-page="tables"><i class="fas fa-table"></i> Tables</div>
      <div class="tab" data-page="dietary"><i class="fas fa-apple-alt"></i> Dietary</div>
      <div class="tab" data-page="guestlist"><i class="fas fa-users"></i> Guest List</div>
      <div class="tab" data-page="runsheet"><i class="fas fa-clock"></i> Run Sheet</div>
      <div class="tab" data-page="summary"><i class="fas fa-clipboard-list"></i> Summary</div>
      <div class="tab nextTab" id="nextTab">Next <i class="fas fa-arrow-right"></i></div>
    </div>
  </div>

  <main>
    <!-- QUOTE PAGE -->
    <section class="page active" id="page-quote">
      <div class="grid">
        <div class="card">
          <h2><i class="fas fa-calculator gold"></i> Indicative Quote (Live)</h2>
          <div class="hint">Room hire + dry hire + security + selected food courses + selected drinks = estimate.</div>
          <div class="row">
            <div><label for="adults">Adults (auto from Tables)</label><input id="adults" type="number" readonly style="background:#f5f5f5;"></div>
            <div><label for="children">Children (auto from Tables)</label><input id="children" type="number" readonly style="background:#f5f5f5;"></div>
          </div>
          <label for="roomHire">Room Hire (AUD)</label><input id="roomHire" type="number" min="0" step="50" value="0">
          <div class="row"><div><label for="dryHire">Dry Hire (AUD)</label><input id="dryHire" type="number" min="0" step="50" value="0"></div><div><label for="security">Security (AUD)</label><input id="security" type="number" min="0" step="50" value="0"></div></div>
          <table class="table"><tbody><tr><td>Room hire</td><td id="qRoom">$0.00</td></tr><tr><td>Food (course-driven)</td><td id="qFood">$0.00</td></tr><tr><td>Drinks</td><td id="qDrinks">$0.00</td></tr><tr><td>Dry hire</td><td id="qDry">$0.00</td></tr><tr><td>Security</td><td id="qSec">$0.00</td></tr></tbody></table>
          <div class="total"><div>Total Estimated Cost</div><div id="qTotal">$0.00</div></div>
          <div class="actions"><button class="btn" id="resetAll"><i class="fas fa-undo-alt"></i> Reset Everything</button><button class="btn" id="printQuote"><i class="fas fa-print"></i> Print</button><button class="btn primary" id="nextQ">Next <i class="fas fa-arrow-right"></i></button></div>
        </div>
        <div class="card">
          <h2><i class="fas fa-chart-pie gold"></i> Quick Snapshot</h2>
          <div class="statusRow"><span class="badge" id="foodCompleteBadge"><i class="fas fa-utensils"></i> Menu: Custom</span><span class="badge" id="drinksCompleteBadge"><i class="fas fa-wine-glass-alt"></i> Drinks: Draft</span></div>
          <div class="kpi"><div class="box"><div class="label">Adults</div><div class="value" id="kpiAdults">0</div></div><div class="box"><div class="label">Children</div><div class="value" id="kpiChildren">0</div></div><div class="box"><div class="label">Food subtotal</div><div class="value" id="kpiFood">$0.00</div></div><div class="box"><div class="label">Avg per guest</div><div class="value" id="kpiAvg">$0.00</div></div></div>
        </div>
      </div>
    </section>

    <!-- COUPLE PAGE -->
    <section class="page" id="page-couple">
      <div class="grid">
        <div class="card">
          <h2><i class="fas fa-user-friends gold"></i> Couple Details</h2>
          <label for="coupleName">Couple Name</label><input id="coupleName" placeholder="e.g., Laura & Daniel">
          <div class="row"><div><label for="weddingDate">Date</label><input id="weddingDate" type="date"></div><div><label for="venueSpace">Venue</label><select id="venueSpace"><option value="sala">Sala Reggio Ballroom</option><option value="members">Members Lounge Restaurant</option><option value="fior">FIOR Parkville</option><option value="alora">ALORA Macedon</option></select></div></div>
          <label for="email">Email</label><input id="email" type="email"><label for="phone">Phone</label><input id="phone" type="tel"><label for="notes">Notes</label><textarea id="notes"></textarea>
          <div class="actions"><button class="btn ghost" id="backC"><i class="fas fa-arrow-left"></i> Back</button><button class="btn primary" id="nextC">Next <i class="fas fa-arrow-right"></i></button></div>
        </div>
        <div class="card"><h2><i class="fas fa-map-marker-alt gold"></i> Venue Profile</h2><div id="venueProfileText">Select a venue</div></div>
      </div>
    </section>

    <!-- FOOD BUILDER (original) -->
    <section class="page" id="page-food">
      <div class="card">
        <h2><i class="fas fa-utensils gold"></i> Food Builder</h2>
        <div class="hint" id="serviceInclusionNote"></div>
        <div class="actions"><button class="btn" onclick="window.selectAll(true)">Select All (max)</button><button class="btn" onclick="window.selectAll(false)">Deselect All</button></div>
        <div class="course" data-course="canapes"><h3>Canapés (max 3)</h3><div class="optionsGrid" id="list-canapes"></div><div class="count" id="count-canapes"></div><div class="warnBox" id="warn-canapes"></div></div>
        <div class="course" data-course="antipasto"><h3>Antipasto (max 2 hot, 5 cold)</h3><div class="optionsGrid" id="list-antipasto"></div><div class="count" id="count-antipasto"></div><div class="warnBox" id="warn-antipasto"></div></div>
        <div class="course" data-course="pasta"><h3>Pasta (max 2)</h3><div class="optionsGrid" id="list-pasta"></div><div class="count" id="count-pasta"></div><div class="warnBox" id="warn-pasta"></div></div>
        <div class="course" data-course="mains"><h3>Mains (max 2)</h3><div class="optionsGrid" id="list-mains"></div><div class="count" id="count-mains"></div><div class="warnBox" id="warn-mains"></div></div>
        <div class="course" data-course="dessert"><h3>Dessert (max 1)</h3><div class="optionsGrid" id="list-dessert"></div><div class="count" id="count-dessert"></div><div class="warnBox" id="warn-dessert"></div></div>
        <div class="course"><h3>Supper</h3><div id="supperNote"></div><div id="supperIncluded"></div><h3>Fruit Platter</h3><label><input id="fruitPlatter" type="checkbox"> Add Fruit Platter</label><div class="row"><div><label>Tables</label><input id="tables" type="number" value="10"></div><div><label>Price/table</label><input id="fruitPerTable" type="number" value="35" readonly></div></div></div>
        <div class="actions"><button class="btn ghost" id="backF2">Back</button><button class="btn primary" id="nextF2">Next</button></div>
      </div>
    </section>

    <!-- DRINKS PAGE -->
    <section class="page" id="page-drinks">
      <div class="grid">
        <div class="card">
          <h2><i class="fas fa-wine-glass-alt gold"></i> Drinks</h2>
          <label for="drinkMode">Drinks Option</label><select id="drinkMode"><option value="bar">Bar Prices</option><option value="custom">Custom per Adult</option></select>
          <div class="row" id="customDrinkRow" style="display:none"><div><label>Price per adult</label><input id="customDrinkPrice" type="number" value="0"></div><div><label>Notes</label><input id="customDrinkNote"></div></div>
          <div class="hint" id="drinkInfo"></div>
          <div class="actions"><button class="btn ghost" id="backD">Back</button><button class="btn primary" id="nextD">Next</button></div>
        </div>
        <div class="card"><h2><i class="fas fa-info-circle gold"></i> Status</h2><div class="statusRow"><span class="badge" id="drinksCompleteBadge2">Drinks: Draft</span></div></div>
      </div>
    </section>

    <!-- TABLE BUILDER (36 + bridal) -->
    <section class="page" id="page-tables">
      <div class="card">
        <h2><i class="fas fa-table gold"></i> Guest Table Builder</h2>
        <div class="hint">Set adults & children per table (0–12 each).</div>
        <div class="actions" style="margin-bottom:15px;"><button id="resetTablesBtn" class="btn"><i class="fas fa-trash-alt"></i> Reset All Tables (Guests)</button></div>
        <div id="tablesGrid" class="table-builder-grid"></div>
        <div class="summary-stats" style="background:var(--soft-gold); padding:16px; border-radius:var(--radius-md); margin-top:20px; display:flex; justify-content:space-between; flex-wrap:wrap; gap:15px; font-weight:700;">
          Total Adults: <span id="totalAdultsTables">0</span> | Total Children: <span id="totalChildrenTables">0</span> | Total Guests: <span id="totalGuestsTables">0</span>
        </div>
        <div class="actions"><button class="btn ghost" id="backTables">Back</button><button class="btn primary" id="nextTables">Next: Dietary</button></div>
      </div>
    </section>

    <!-- DIETARY PAGE (numeric 0-12 per requirement) -->
    <section class="page" id="page-dietary">
      <div class="card">
        <h2><i class="fas fa-apple-alt gold"></i> Dietary Requirements by Table</h2>
        <div class="hint">Enter number of guests (0–12) with each dietary need per table.</div>
        <div class="actions" style="margin-bottom:15px;"><button id="resetDietaryBtn" class="btn"><i class="fas fa-trash-alt"></i> Reset All Dietary</button></div>
        <div id="dietaryGrid" class="table-builder-grid"></div>
        <div class="actions"><button class="btn ghost" id="backDietary">Back</button><button class="btn primary" id="nextDietary">Next: Guest List</button></div>
      </div>
    </section>

    <!-- GUEST LIST MAKER (up to 12 names per table) -->
    <section class="page" id="page-guestlist">
      <div class="card">
        <h2><i class="fas fa-users gold"></i> Guest List Maker – Seating by Table</h2>
        <div class="hint">Enter up to 12 guest names per table. Exactly where they are seated.</div>
        <div class="actions" style="margin-bottom:15px;"><button id="resetGuestListBtn" class="btn"><i class="fas fa-trash-alt"></i> Reset All Guest Lists</button></div>
        <div id="guestListGrid" class="table-builder-grid"></div>
        <div class="actions"><button class="btn ghost" id="backGuestList">Back</button><button class="btn primary" id="nextGuestList">Next: Run Sheet</button></div>
      </div>
    </section>

    <!-- RUN SHEET (exact minute selection) -->
    <section class="page" id="page-runsheet">
      <div class="card">
        <h2><i class="fas fa-clock gold"></i> Run Sheet / Timeline</h2>
        <div class="hint">Add timeline entries. Select exact hour and minute using dropdowns.</div>
        <button id="addRunRowBtn" class="btn small" style="margin-bottom:15px;"><i class="fas fa-plus"></i> Add Activity</button>
        <table class="run-sheet-table"><thead><tr><th>Time (Hour : Minute)</th><th>Activity</th><th style="width:60px"></th></tr></thead><tbody id="runSheetBody"></tbody></table>
        <div class="actions"><button class="btn ghost" id="backRun">Back</button><button class="btn primary" id="nextRun">Next: Summary</button></div>
      </div>
    </section>

    <!-- SUMMARY PAGE (includes guest list) -->
    <section class="page" id="page-summary">
      <div class="grid">
        <div class="card">
          <h2><i class="fas fa-clipboard-check gold"></i> Final Summary</h2>
          <div id="finalSummary"></div>
          <div class="actions"><button id="copySummary">Copy</button><button id="printSummary">Print</button><button class="btn ghost" id="backS">Back</button><button class="btn primary" id="toQuote">Back to Quote</button></div>
        </div>
        <div class="card"><h2><i class="fas fa-tags gold"></i> Selected Items</h2><div id="chips"></div><div id="summaryCourseCounts"></div></div>
      </div>
    </section>
  </main>

  <footer id="brandFooter">Members Lounge | Reggio Calabria Club, Parkville</footer>

<script>
  // ========== FULL JAVASCRIPT – ALL MODULES ==========
  function escapeHtml(str){ return String(str||"").replace(/[&<>]/g, function(m){if(m==='&') return '&amp;'; if(m==='<') return '&lt;'; if(m==='>') return '&gt;'; return m;}); }
  const $ = id => document.getElementById(id);
  function on(el, ev, fn){ if(el) el.addEventListener(ev, fn); }

  const VENUE_PROFILES = {
    sala: { label:"Sala Reggio Ballroom", logo:"members_lounge_logo.png", subtitle:"Reggio Calabria Club, Parkville", footer:"Members Lounge | Reggio Calabria Club, Parkville", defaults:{roomHire:0,dryHire:0,security:0} },
    members: { label:"Members Lounge Restaurant", logo:"members_lounge_logo.png", subtitle:"Reggio Calabria Club, Parkville", footer:"Members Lounge | Reggio Calabria Club, Parkville", defaults:{roomHire:0,dryHire:0,security:0} },
    fior: { label:"FIOR Parkville", logo:"fior.png", subtitle:"FIOR, Parkville", footer:"FIOR | Parkville", defaults:{roomHire:0,dryHire:0,security:0} },
    alora: { label:"ALORA Macedon", logo:"alora.png", subtitle:"ALORA, Macedon", footer:"ALORA | Macedon", defaults:{roomHire:0,dryHire:0,security:0} }
  };
  const BASE_MENU = {
    canapes:["Arancini","Focaccia fingers","Prawn spring rolls","Parmigiano bites","Caprese empanadas","Beef empanadas"],
    antipasto:["Bruschetta di pomodoro","Salumi selection","Feta & Kefalograviera","Tiropita","Trio of dips with pita","Warm marinated olives","Eggplant parmigiana","Bocconcini & ricotta","Calamari fritti","Meatballs in sugo","Cevapi","Italian pork sausages"],
    pasta:["Pasta al Ragú","Gnocchi Pesto","Casarecce Amatriciana","Pasta Napoli"],
    mains:["Chargrilled chicken","Barramundi fillet","Lamb shank","Braised beef short ribs","Bondiola","Stuffed pepper"],
    dessert:["Sharing assorted desserts","Tiramisu & cheese cake","Gelato dessert"]
  };
  const ALORA_MENU = { canapes:["Pizzette","Mini caprese empanadas","Pork & fennel sausage rolls","Parmigiano bites","Crumb sandwiches","Mini beef empanadas"], antipasto:["Bruschetta","Salumi","Feta & Kefalograviera","Trio of dips","Warm olives","Bocconcini & ricotta","Eggplant caponata","Chargrilled prawns","Italian pork sausages","Meatballs"], pasta:["Pasta al Ragú","Baked gnocchi caprese"], mains:BASE_MENU.mains, dessert:BASE_MENU.dessert };
  function getMenuForVenue(v){ return v==="alora"?ALORA_MENU:BASE_MENU; }
  const HOT_ANTIPASTO = {"Calamari fritti":true,"Chargrilled prawns":true,"Cevapi":true,"Meatballs in sugo":true,"Italian pork sausages":true};
  const COURSE_LABEL = { canapes:"Canapés", antipasto:"Antipasto", pasta:"Pasta", mains:"Mains", dessert:"Dessert" };
  const RULES = { canapes:{max:3}, antipasto:{max:7}, pasta:{max:2}, mains:{max:2}, dessert:{max:1} };
  const FIXED_PRICES = { canapes:12, antipasto:28, pasta:15, mains:34, dessert:10 };
  const TAB_ORDER = ["quote","couple","food","drinks","tables","dietary","guestlist","runsheet","summary"];
  const SERVICE_INCLUSION_TEXT = "Dinner rolls, butter, garden salad, seasonal vegetables included.";
  const SUPPER_BY_VENUE = { sala:{note:"Pizza only",included:"Pizza"}, members:{note:"Pizza only",included:"Pizza"}, fior:{note:"Pizza only",included:"Pizza"}, alora:{note:"No supper",included:"No supper"} };

  const adultsEl = $("adults"), childrenEl = $("children"), roomHireEl = $("roomHire"), dryHireEl = $("dryHire"), securityEl = $("security");
  const fruitPlatterEl = $("fruitPlatter"), tablesEl = $("tables"), fruitPerTableEl = $("fruitPerTable");
  const drinkModeEl = $("drinkMode"), customDrinkRow = $("customDrinkRow"), customDrinkPriceEl = $("customDrinkPrice"), customDrinkNoteEl = $("customDrinkNote");
  const coupleNameEl = $("coupleName"), weddingDateEl = $("weddingDate"), venueSpace = $("venueSpace");
  const qRoom=$("qRoom"),qFood=$("qFood"),qDrinks=$("qDrinks"),qDry=$("qDry"),qSec=$("qSec"),qTotal=$("qTotal");
  const kpiAdults=$("kpiAdults"),kpiChildren=$("kpiChildren"),kpiFood=$("kpiFood"),kpiAvg=$("kpiAvg");
  const finalSummary=$("finalSummary"), chips=$("chips"), summaryCourseCounts=$("summaryCourseCounts");
  const foodCompleteBadge=$("foodCompleteBadge"), drinksCompleteBadge=$("drinksCompleteBadge"), drinksCompleteBadge2=$("drinksCompleteBadge2");

  function aud(n){ return (Number(n||0)).toLocaleString("en-AU",{style:"currency",currency:"AUD"}); }
  function setBadge(el,state,text){ if(!el) return; el.classList.remove("ok","warn"); if(state) el.classList.add(state); const i=el.querySelector("i"); el.innerHTML=""; if(i){ el.appendChild(i); el.appendChild(document.createTextNode(" "+text)); } else el.textContent=text; }
  function updateDrinkUI(){ customDrinkRow.style.display=drinkModeEl.value==="custom"?"grid":"none"; $("drinkInfo").textContent=drinkModeEl.value==="bar"?"Bar pricing indicative only (drinks = $0).":"Custom price adds per adult."; renderAll(); }
  function getSelections(){ const out={canapes:[],antipasto:[],pasta:[],mains:[],dessert:[]}; document.querySelectorAll('input[type="checkbox"][data-course]:checked').forEach(cb=>{ if(out[cb.dataset.course]) out[cb.dataset.course].push(cb.value); }); return out; }
  function enforceLimit(courseKey){ if(courseKey==="antipasto"){ enforceAntipastoLimit(); return; } const rule=RULES[courseKey]; if(!rule) return; const checked=Array.from(document.querySelectorAll(`input[data-course="${courseKey}"]:checked`)); if(checked.length>rule.max) checked[checked.length-1].checked=false; const now=Array.from(document.querySelectorAll(`input[data-course="${courseKey}"]:checked`)); const countEl=$( `count-${courseKey}`); if(countEl) countEl.textContent=`${now.length} selected (max ${rule.max})`; const warnEl=$( `warn-${courseKey}`); if(warnEl){ warnEl.style.display=now.length>=rule.max?"block":"none"; if(now.length>=rule.max) warnEl.textContent=`Maximum ${rule.max} selections for ${COURSE_LABEL[courseKey]}.`; } }
  function enforceAntipastoLimit(){ const checkboxes=document.querySelectorAll('input[data-course="antipasto"]'); let hot=[],cold=[]; checkboxes.forEach(cb=>{ if(cb.checked) HOT_ANTIPASTO[cb.value]?hot.push(cb):cold.push(cb); }); if(hot.length>2) hot[hot.length-1].checked=false; if(cold.length>5) cold[cold.length-1].checked=false; hot=[];cold=[]; checkboxes.forEach(cb=>{ if(cb.checked) HOT_ANTIPASTO[cb.value]?hot.push(cb):cold.push(cb); }); const countEl=$("count-antipasto"); if(countEl) countEl.textContent=`Hot: ${hot.length}/2 · Cold: ${cold.length}/5`; const warnEl=$("warn-antipasto"); if(warnEl) warnEl.style.display=(hot.length===2||cold.length===5)?"block":"none"; }
  function attachLimitListeners(){ document.querySelectorAll('input[type="checkbox"][data-course]').forEach(cb=>{ cb.removeEventListener("change",limitHandler); cb.addEventListener("change",limitHandler); }); }
  function limitHandler(e){ enforceLimit(e.target.dataset.course); renderAll(); }
  window.selectAll = function(checked){ if(checked){ ["canapes","pasta","mains","dessert"].forEach(c=>{ const items=document.querySelectorAll(`input[data-course="${c}"]`); for(let i=0;i<Math.min(items.length,RULES[c].max);i++) items[i].checked=true; enforceLimit(c); }); const anti=Array.from(document.querySelectorAll('input[data-course="antipasto"]')); const hot=anti.filter(cb=>HOT_ANTIPASTO[cb.value]), cold=anti.filter(cb=>!HOT_ANTIPASTO[cb.value]); hot.slice(0,2).forEach(cb=>cb.checked=true); cold.slice(0,5).forEach(cb=>cb.checked=true); enforceAntipastoLimit(); } else { document.querySelectorAll('input[type="checkbox"][data-course]').forEach(i=>i.checked=false); Object.keys(RULES).forEach(enforceLimit); enforceAntipastoLimit(); } renderAll(); };
  function rebuildFoodLists(){ const venue=venueSpace.value; const menu=getMenuForVenue(venue); ["canapes","antipasto","pasta","mains","dessert"].forEach(c=>addCheckboxList(c,`list-${c}`,menu[c])); attachLimitListeners(); enforceAntipastoLimit(); Object.keys(RULES).forEach(enforceLimit); renderAll(); }
  function addCheckboxList(course,listId,items){ const wrap=$(listId); if(!wrap) return; wrap.innerHTML=""; items.forEach((item,idx)=>{ const div=document.createElement("div"); div.className="opt"; const isHot=(course==="antipasto"&&HOT_ANTIPASTO[item]); const id=`cb-${course}-${idx}`; div.innerHTML=`<input type="checkbox" data-course="${course}" value="${escapeHtml(item)}" id="${id}"><span class="optNum">${idx+1}</span><label for="${id}" class="optLabel">${escapeHtml(item)}${isHot?'<span class="hotTag">HOT</span>':''}</label>`; wrap.appendChild(div); }); }
  function calculateFood(){ const adults=Number(adultsEl.value||0), children=Number(childrenEl.value||0); const sel=getSelections(); let perAdult=0; if(sel.canapes.length) perAdult+=FIXED_PRICES.canapes; if(sel.antipasto.length) perAdult+=FIXED_PRICES.antipasto; if(sel.pasta.length) perAdult+=FIXED_PRICES.pasta; if(sel.mains.length) perAdult+=FIXED_PRICES.mains; if(sel.dessert.length) perAdult+=FIXED_PRICES.dessert; const adultTotal=adults*perAdult, childTotal=children*perAdult*0.3; let fruitTotal=0; if(fruitPlatterEl&&fruitPlatterEl.checked) fruitTotal=Number(tablesEl.value||0)*Number(fruitPerTableEl.value||0); return adultTotal+childTotal+fruitTotal; }
  function calculateDrinks(){ const adults=Number(adultsEl.value||0); return drinkModeEl.value==="custom"?adults*Number(customDrinkPriceEl.value||0):0; }

  // ========== TABLE BUILDER ==========
  const TOTAL_STD_TABLES = 36;
  let tableData = [];
  function initTableData(){ tableData=[]; for(let i=1;i<=TOTAL_STD_TABLES;i++) tableData.push({ name:`Table ${i}`, adults:0, children:0 }); tableData.push({ name:`👰 Bridal Table`, adults:0, children:0 }); const saved=localStorage.getItem('tableBuilderData'); if(saved) try{ let p=JSON.parse(saved); if(p.length===tableData.length) tableData=p; }catch(e){} }
  function saveTableData(){ localStorage.setItem('tableBuilderData',JSON.stringify(tableData)); }
  function updateGuestTotalsFromTables(){ let totalAdults=tableData.reduce((s,t)=>s+(parseInt(t.adults)||0),0); let totalChildren=tableData.reduce((s,t)=>s+(parseInt(t.children)||0),0); if(adultsEl) adultsEl.value=totalAdults; if(childrenEl) childrenEl.value=totalChildren; document.getElementById('totalAdultsTables')&&(document.getElementById('totalAdultsTables').innerText=totalAdults); document.getElementById('totalChildrenTables')&&(document.getElementById('totalChildrenTables').innerText=totalChildren); document.getElementById('totalGuestsTables')&&(document.getElementById('totalGuestsTables').innerText=totalAdults+totalChildren); if(adultsEl) adultsEl.dispatchEvent(new Event('input')); if(childrenEl) childrenEl.dispatchEvent(new Event('input')); }
  function renderTableBuilder(){ const container=document.getElementById('tablesGrid'); if(!container) return; let html=''; tableData.forEach((t,idx)=>{ html+=`<div class="table-card"><div class="table-title">${escapeHtml(t.name)}</div><div class="table-controls"><select class="table-adults" data-idx="${idx}">${generateOptions(t.adults)}</select><select class="table-children" data-idx="${idx}">${generateOptions(t.children)}</select></div></div>`; }); container.innerHTML=html; document.querySelectorAll('.table-adults').forEach(sel=>{ sel.addEventListener('change',(e)=>{ let idx=parseInt(sel.dataset.idx); tableData[idx].adults=parseInt(sel.value)||0; saveTableData(); updateGuestTotalsFromTables(); renderAll(); }); }); document.querySelectorAll('.table-children').forEach(sel=>{ sel.addEventListener('change',(e)=>{ let idx=parseInt(sel.dataset.idx); tableData[idx].children=parseInt(sel.value)||0; saveTableData(); updateGuestTotalsFromTables(); renderAll(); }); }); }
  function resetAllTables(){ if(confirm("Reset all table guest counts to zero?")){ tableData.forEach(t=>{ t.adults=0; t.children=0; }); saveTableData(); renderTableBuilder(); updateGuestTotalsFromTables(); renderAll(); } }
  function generateOptions(selected){ let opts=''; for(let i=0;i<=12;i++) opts+=`<option value="${i}" ${selected===i?'selected':''}>${i}</option>`; return opts; }

  // ========== DIETARY ==========
  let dietaryData = [];
  const dietTypes = ['vegetarian','vegan','gluten','dairyFree','fructose','fodmap','nut'];
  function initDietaryData(){ dietaryData=[]; for(let i=0;i<tableData.length;i++){ let obj={ tableName:tableData[i].name, vegetarian:0, vegan:0, gluten:0, dairyFree:0, fructose:0, fodmap:0, nut:0, notes:'' }; dietaryData.push(obj); } const saved=localStorage.getItem('dietaryData'); if(saved) try{ let p=JSON.parse(saved); if(p.length===dietaryData.length) dietaryData=p; }catch(e){} }
  function saveDietaryData(){ localStorage.setItem('dietaryData',JSON.stringify(dietaryData)); }
  function renderDietaryTable(){ const container=document.getElementById('dietaryGrid'); if(!container) return; let html=''; dietaryData.forEach((d,idx)=>{ html+=`<div class="table-card"><div class="table-title">${escapeHtml(d.tableName)}</div><div class="dietary-grid">`; dietTypes.forEach(type=>{ let label=type==='gluten'?'Gluten Free':type==='dairyFree'?'Dairy Free':type==='fodmap'?'FODMAP':type==='nut'?'Nut Allergy':type.charAt(0).toUpperCase()+type.slice(1); html+=`<div class="dietary-item"><label>${label}</label><input type="number" min="0" max="12" value="${d[type]}" class="diet-count" data-idx="${idx}" data-type="${type}" style="width:60px;"></div>`; }); html+=`<textarea class="diet-notes" data-idx="${idx}" placeholder="Notes..." rows="2">${escapeHtml(d.notes)}</textarea></div></div>`; }); container.innerHTML=html; document.querySelectorAll('.diet-count').forEach(inp=>{ inp.addEventListener('change',(e)=>{ let idx=parseInt(inp.dataset.idx); let type=inp.dataset.type; let val=Math.min(12,Math.max(0,parseInt(inp.value)||0)); dietaryData[idx][type]=val; saveDietaryData(); renderAll(); }); }); document.querySelectorAll('.diet-notes').forEach(ta=>{ ta.addEventListener('change',(e)=>{ let idx=parseInt(ta.dataset.idx); dietaryData[idx].notes=ta.value; saveDietaryData(); renderAll(); }); }); }
  function resetAllDietary(){ if(confirm("Reset all dietary counts to zero?")){ dietaryData.forEach(d=>{ dietTypes.forEach(t=>{ d[t]=0; }); d.notes=''; }); saveDietaryData(); renderDietaryTable(); renderAll(); } }

  // ========== GUEST LIST ==========
  let guestListData = [];
  function initGuestListData() {
    guestListData = [];
    for(let i=0; i<tableData.length; i++) {
      let names = Array(12).fill('');
      guestListData.push({ tableName: tableData[i].name, names: names });
    }
    const saved = localStorage.getItem('guestListData');
    if(saved) {
      try {
        let p = JSON.parse(saved);
        if(p.length === guestListData.length) guestListData = p;
      } catch(e) {}
    }
  }
  function saveGuestListData() { localStorage.setItem('guestListData', JSON.stringify(guestListData)); }
  function renderGuestListTable() {
    const container = document.getElementById('guestListGrid');
    if(!container) return;
    let html = '';
    guestListData.forEach((g, idx) => {
      html += `<div class="table-card"><div class="table-title">${escapeHtml(g.tableName)} – up to 12 guests</div><div class="guest-list-grid">`;
      for(let i=0; i<12; i++) {
        html += `<input type="text" class="guest-name-input" data-idx="${idx}" data-slot="${i}" value="${escapeHtml(g.names[i])}" placeholder="Guest ${i+1}">`;
      }
      html += `</div></div>`;
    });
    container.innerHTML = html;
    document.querySelectorAll('.guest-name-input').forEach(inp => {
      inp.addEventListener('change', (e) => {
        let idx = parseInt(inp.dataset.idx);
        let slot = parseInt(inp.dataset.slot);
        guestListData[idx].names[slot] = inp.value;
        saveGuestListData();
        renderAll();
      });
    });
  }
  function resetAllGuestLists() {
    if(confirm("Reset all guest lists (clear all names)?")) {
      for(let i=0; i<guestListData.length; i++) {
        for(let j=0; j<12; j++) guestListData[i].names[j] = '';
      }
      saveGuestListData();
      renderGuestListTable();
      renderAll();
    }
  }

  // ========== RUN SHEET ==========
  let runSheetRows = [];
  function initRunSheet(){ const saved=localStorage.getItem('runSheetData'); if(saved) try{ runSheetRows=JSON.parse(saved); }catch(e){ runSheetRows=[]; } if(runSheetRows.length===0) runSheetRows=[{ time:'17:00', activity:'Cocktail hour'},{ time:'18:30', activity:'Dinner'},{ time:'21:00', activity:'Last order'}]; }
  function saveRunSheetData(){ localStorage.setItem('runSheetData',JSON.stringify(runSheetRows)); }
  function renderRunSheet(){ const tbody=document.getElementById('runSheetBody'); if(!tbody) return; tbody.innerHTML=''; if(runSheetRows.length===0){ tbody.innerHTML='<tr><td colspan="3" style="text-align:center">No entries. Click Add Activity</td></tr>'; return; } runSheetRows.forEach((row,idx)=>{ const tr=document.createElement('tr'); const tdTime=document.createElement('td'); tdTime.innerHTML=`<div style="display:flex; gap:6px;"><select class="hour-select" data-idx="${idx}" style="padding:6px;">${Array.from({length:24},(_,i)=>{ const h=i.toString().padStart(2,'0'); const selected=(row.time && row.time.split(':')[0]===h)?'selected':''; return `<option value="${h}" ${selected}>${h}</option>`; }).join('')}</select><span>:</span><select class="minute-select" data-idx="${idx}" style="padding:6px;">${Array.from({length:60},(_,i)=>{ const m=i.toString().padStart(2,'0'); const selected=(row.time && row.time.split(':')[1]===m)?'selected':''; return `<option value="${m}" ${selected}>${m}</option>`; }).join('')}</select></div>`; const tdAct=document.createElement('td'); const actInput=document.createElement('input'); actInput.type='text'; actInput.value=row.activity; actInput.placeholder='Activity'; actInput.style.width='100%'; tdAct.appendChild(actInput); const tdDel=document.createElement('td'); const delBtn=document.createElement('button'); delBtn.innerHTML='<i class="fas fa-trash-alt"></i>'; delBtn.className='btn small'; delBtn.style.background='transparent'; delBtn.style.color='#b33'; tdDel.appendChild(delBtn); tr.append(tdTime,tdAct,tdDel); tbody.appendChild(tr); const hourSel=tr.querySelector('.hour-select'); const minSel=tr.querySelector('.minute-select'); const updateTime=()=>{ runSheetRows[idx].time=`${hourSel.value}:${minSel.value}`; saveRunSheetData(); renderAll(); }; hourSel.addEventListener('change',updateTime); minSel.addEventListener('change',updateTime); actInput.addEventListener('change',()=>{ runSheetRows[idx].activity=actInput.value; saveRunSheetData(); renderAll(); }); delBtn.onclick=()=>{ runSheetRows.splice(idx,1); saveRunSheetData(); renderRunSheet(); renderAll(); }; }); }
  function addRunRow(){ runSheetRows.push({ time:'18:00', activity:'' }); saveRunSheetData(); renderRunSheet(); renderAll(); }

  // ========== RENDER ALL ==========
  function renderAll(){
    const adults = Number(adultsEl.value || 0);
    const children = Number(childrenEl.value || 0);
    const totalGuests = adults + children;
    const room = Number(roomHireEl.value || 0);
    const dry = Number(dryHireEl.value || 0);
    const sec = Number(securityEl.value || 0);
    const food = calculateFood();
    const drinks = calculateDrinks();
    const total = room + dry + sec + food + drinks;

    qRoom.textContent = aud(room);
    qDry.textContent = aud(dry);
    qSec.textContent = aud(sec);
    qFood.textContent = aud(food);
    qDrinks.textContent = aud(drinks);
    qTotal.textContent = aud(total);
    kpiAdults.textContent = adults;
    kpiChildren.textContent = children;
    kpiFood.textContent = aud(food);
    kpiAvg.textContent = totalGuests ? aud(total / totalGuests) : aud(0);

    const sel = getSelections();
    const anyFood = Object.values(sel).some(a => a.length);
    setBadge(foodCompleteBadge, anyFood ? "ok" : "warn", anyFood ? "Menu Ready" : "Menu Empty");
    setBadge(drinksCompleteBadge, drinkModeEl.value === "bar" ? "warn" : "ok", drinkModeEl.value === "bar" ? "Bar Pricing" : "Custom Drinks");
    setBadge(drinksCompleteBadge2, drinkModeEl.value === "bar" ? "warn" : "ok", drinkModeEl.value === "bar" ? "Bar Pricing" : "Custom Drinks");

    const coupleName = coupleNameEl.value.trim() || "Event";
    const weddingDate = weddingDateEl.value || "Not set";
    const venueText = venueSpace.options[venueSpace.selectedIndex]?.text || "Not selected";
    const venueKey = venueSpace.value;

    const foodItemsHtml = Object.keys(COURSE_LABEL).map(c => {
      const items = sel[c];
      if(items.length === 0) return "";
      return `<li><strong>${COURSE_LABEL[c]}:</strong> ${items.map(escapeHtml).join(", ")}</li>`;
    }).filter(Boolean).join("");
    const fruitHtml = fruitPlatterEl.checked ? `<li><strong>Fruit platter:</strong> ${tablesEl.value} tables × ${aud(fruitPerTableEl.value)} = ${aud(Number(tablesEl.value)*Number(fruitPerTableEl.value))}</li>` : "";
    const supperSummary = venueKey === "alora" ? "No supper" : "Pizza supper included";
    const serviceHtml = `<li><strong>Service:</strong> ${SERVICE_INCLUSION_TEXT}</li><li><strong>Supper:</strong> ${supperSummary}</li>`;
    let drinkDetails = drinkModeEl.value === "bar" ? "Bar prices (no package)" : `Custom: ${aud(customDrinkPriceEl.value)}/adult × ${adults} = ${aud(Number(customDrinkPriceEl.value)*adults)}`;

    let summaryHtml = `
      <div class="summary-section"><div class="summary-line"><span>Event:</span><span>${escapeHtml(coupleName)}</span></div>
      <div class="summary-line"><span>Date:</span><span>${escapeHtml(weddingDate)}</span></div>
      <div class="summary-line"><span>Venue:</span><span>${escapeHtml(venueText)}</span></div>
      <div class="summary-line"><span>Guests:</span><span>${adults} adults, ${children} children</span></div></div>
      <div class="summary-section"><h3>Food</h3>${(foodItemsHtml||fruitHtml||serviceHtml)?`<ul class="summary-items">${foodItemsHtml}${fruitHtml}${serviceHtml}</ul>`:"<p>No food</p>"}<div class="summary-line"><span>Food subtotal:</span><span>${aud(food)}</span></div></div>
      <div class="summary-section"><h3>Drinks</h3><div class="summary-line"><span>Mode:</span><span>${drinkDetails}</span></div><div class="summary-line"><span>Drinks total:</span><span>${aud(drinks)}</span></div></div>
      <div class="summary-section"><h3>Other costs</h3><div class="summary-line"><span>Room hire:</span><span>${aud(room)}</span></div><div class="summary-line"><span>Dry hire:</span><span>${aud(dry)}</span></div><div class="summary-line"><span>Security:</span><span>${aud(sec)}</span></div></div>
      <div class="summary-line total"><span>Grand total</span><span>${aud(total)}</span></div>
    `;

    let tableBreakdown = '<div class="summary-section"><h3>📊 Table Breakdown</h3><ul>';
    tableData.forEach(t => { if(t.adults>0||t.children>0) tableBreakdown += `<li>${escapeHtml(t.name)}: ${t.adults} adults, ${t.children} children</li>`; });
    tableBreakdown += '</ul></div>';

    let dietarySummary = '<div class="summary-section"><h3>🌿 Dietary Requirements</h3><ul>';
    dietaryData.forEach(d => {
      let entries = [];
      if(d.vegetarian>0) entries.push(`Vegetarian: ${d.vegetarian}`);
      if(d.vegan>0) entries.push(`Vegan: ${d.vegan}`);
      if(d.gluten>0) entries.push(`Gluten Free: ${d.gluten}`);
      if(d.dairyFree>0) entries.push(`Dairy Free: ${d.dairyFree}`);
      if(d.fructose>0) entries.push(`Fructose: ${d.fructose}`);
      if(d.fodmap>0) entries.push(`FODMAP: ${d.fodmap}`);
      if(d.nut>0) entries.push(`Nut Allergy: ${d.nut}`);
      if(entries.length||d.notes) dietarySummary += `<li><strong>${escapeHtml(d.tableName)}</strong>: ${entries.join(', ')} ${d.notes?`— ${escapeHtml(d.notes)}`:''}</li>`;
    });
    dietarySummary += '</ul></div>';

    let guestSummary = '<div class="summary-section"><h3>📋 Seating Plan (Guest List)</h3>';
    guestListData.forEach(g => {
      let nonEmpty = g.names.filter(n => n.trim() !== '');
      if(nonEmpty.length) {
        guestSummary += `<div><strong>${escapeHtml(g.tableName)}</strong>: ${nonEmpty.map(escapeHtml).join(', ')}</div>`;
      }
    });
    guestSummary += '</div>';

    let runSheetHtml = '<div class="summary-section"><h3>⏱️ Run Sheet</h3><ul>';
    runSheetRows.forEach(r => { if(r.time||r.activity) runSheetHtml += `<li><strong>${escapeHtml(r.time)}</strong> — ${escapeHtml(r.activity)}</li>`; });
    runSheetHtml += '</ul></div>';

    finalSummary.innerHTML = summaryHtml + tableBreakdown + dietarySummary + guestSummary + runSheetHtml;

    chips.innerHTML = "";
    Object.entries(sel).forEach(([c,items])=>items.forEach(item=>{ let chip=document.createElement("span"); chip.className="pill"; chip.textContent=item; chips.appendChild(chip); }));
    if(fruitPlatterEl.checked){ let chip=document.createElement("span"); chip.className="pill"; chip.textContent=`Fruit platter (${tablesEl.value} tables)`; chips.appendChild(chip); }
    let supperChip=document.createElement("span"); supperChip.className="pill"; supperChip.textContent=supperSummary; chips.appendChild(supperChip);
    let serviceChip=document.createElement("span"); serviceChip.className="pill"; serviceChip.textContent="Dinner rolls, butter, salad, veg"; chips.appendChild(serviceChip);
    let counts=Object.keys(COURSE_LABEL).map(c=>{ if(c==="antipasto"){ let hot=sel.antipasto.filter(i=>HOT_ANTIPASTO[i]).length, cold=sel.antipasto.length-hot; return `Antipasto: ${hot} hot, ${cold} cold`; } else return `${COURSE_LABEL[c]}: ${sel[c].length}`; }).join(" · ");
    summaryCourseCounts.textContent = counts;
  }

  function applyVenueProfile(key){ const p=VENUE_PROFILES[key]||VENUE_PROFILES.members; $("brandSubtitle").textContent=p.subtitle; $("brandFooter").textContent=p.footer; const img=$("brandLogo"); img.onerror=function(){ img.style.display="none"; $("logoFallback").style.display="inline-flex"; }; img.src=p.logo; const info=$("venueProfileText"); if(info) info.innerHTML=`<b>Active:</b> ${p.label}<br>${key==="alora"?"Limited menu, no fried items, no supper.":"Full menu"}`; const r=Number(roomHireEl.value||0), d=Number(dryHireEl.value||0), s=Number(securityEl.value||0); if(roomHireEl&&r===0) roomHireEl.value=p.defaults.roomHire; if(dryHireEl&&d===0) dryHireEl.value=p.defaults.dryHire; if(securityEl&&s===0) securityEl.value=p.defaults.security; const supperConfig=SUPPER_BY_VENUE[key]||SUPPER_BY_VENUE.members; if($("supperNote")) $("supperNote").textContent=supperConfig.note; if($("supperIncluded")) $("supperIncluded").textContent=supperConfig.included; if($("serviceInclusionNote")) $("serviceInclusionNote").textContent=SERVICE_INCLUSION_TEXT; rebuildFoodLists(); }
  function resetAll(){
    if(!confirm("Reset everything?")) return;
    adultsEl.value=0; childrenEl.value=0; roomHireEl.value=0; dryHireEl.value=0; securityEl.value=0; tablesEl.value=10; if(fruitPlatterEl) fruitPlatterEl.checked=false; drinkModeEl.value="bar"; customDrinkPriceEl.value=0; customDrinkNoteEl.value=""; coupleNameEl.value=""; weddingDateEl.value=""; $("email").value=""; $("phone").value=""; $("notes").value=""; venueSpace.value="members";
    Object.keys(RULES).forEach(enforceLimit); rebuildFoodLists(); updateDrinkUI(); applyVenueProfile("members");
    initTableData(); initDietaryData(); initGuestListData(); initRunSheet();
    renderTableBuilder(); renderDietaryTable(); renderGuestListTable(); renderRunSheet();
    updateGuestTotalsFromTables(); renderAll();
  }

  function setActivePage(page){
    document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));
    $(`page-${page}`).classList.add("active");
    document.querySelectorAll(".tab[data-page]").forEach(t=>{ t.classList.toggle("active",t.dataset.page===page); });
    document.querySelector("main").scrollTop=0;
    if(page==='tables') renderTableBuilder();
    if(page==='dietary') renderDietaryTable();
    if(page==='guestlist') renderGuestListTable();
    if(page==='runsheet') renderRunSheet();
    if(page==='summary') renderAll();
  }
  function nextPage(){ const cur=document.querySelector(".tab[data-page].active")?.dataset.page||"quote"; const idx=TAB_ORDER.indexOf(cur); setActivePage(TAB_ORDER[(idx+1)%TAB_ORDER.length]); }
  function prevPage(){ const cur=document.querySelector(".tab[data-page].active")?.dataset.page||"quote"; const idx=TAB_ORDER.indexOf(cur); setActivePage(TAB_ORDER[(idx-1+TAB_ORDER.length)%TAB_ORDER.length]); }

  document.querySelectorAll(".tab[data-page]").forEach(t=>on(t,"click",()=>setActivePage(t.dataset.page)));
  on($("nextTab"),"click",nextPage);
  on($("nextQ"),"click",nextPage);
  on($("backC"),"click",prevPage);
  on($("nextC"),"click",nextPage);
  on($("backF2"),"click",prevPage);
  on($("nextF2"),"click",nextPage);
  on($("backD"),"click",prevPage);
  on($("nextD"),"click",nextPage);
  on($("backTables"),"click",()=>setActivePage("drinks"));
  on($("nextTables"),"click",()=>setActivePage("dietary"));
  on($("backDietary"),"click",()=>setActivePage("tables"));
  on($("nextDietary"),"click",()=>setActivePage("guestlist"));
  on($("backGuestList"),"click",()=>setActivePage("dietary"));
  on($("nextGuestList"),"click",()=>setActivePage("runsheet"));
  on($("backRun"),"click",()=>setActivePage("guestlist"));
  on($("nextRun"),"click",()=>setActivePage("summary"));
  on($("backS"),"click",()=>setActivePage("runsheet"));
  on($("toQuote"),"click",()=>setActivePage("quote"));

  on($("copySummary"),"click",async()=>{ try{ await navigator.clipboard.writeText(finalSummary.innerText); alert("Copied"); }catch(e){ alert("Failed"); } });
  on($("printSummary"),"click",()=>window.print());
  on($("resetAll"),"click",resetAll);
  on($("printQuote"),"click",()=>window.print());
  on(drinkModeEl,"change",updateDrinkUI);
  on(venueSpace,"change",()=>applyVenueProfile(venueSpace.value));
  document.querySelectorAll("input, select, textarea").forEach(el=>{ el.addEventListener("input",renderAll); el.addEventListener("change",renderAll); });

  on($("resetTablesBtn"),"click",resetAllTables);
  on($("resetDietaryBtn"),"click",resetAllDietary);
  on($("resetGuestListBtn"),"click",resetAllGuestLists);
  on($("addRunRowBtn"),"click",addRunRow);

  // Initialisation
  initTableData(); initDietaryData(); initGuestListData(); initRunSheet();
  renderTableBuilder(); renderDietaryTable(); renderGuestListTable(); renderRunSheet();
  updateGuestTotalsFromTables();
  rebuildFoodLists(); updateDrinkUI(); applyVenueProfile("members"); renderAll();
  console.log("%c✅ Full suite loaded: Table Builder, Dietary (0-12), Guest List (12 names/table), Run Sheet (minute precise).", "color:#C6A43F; font-weight:700;");
</script>
</body>
</html>